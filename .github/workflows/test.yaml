name: Test .NET solution or project
permissions:
  packages: read
on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  test:
    uses: devantler-tech/reusable-workflows/.github/workflows/ci-dotnet-test.yaml@97d2137ad2f726fadd2d1430b4c428e28a036819 # v1.14.0
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
