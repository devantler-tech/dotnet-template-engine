name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/cd-dotnet-library-publish.yaml@97d2137ad2f726fadd2d1430b4c428e28a036819 # v1.14.0
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
