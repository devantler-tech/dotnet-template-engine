name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/cd-dotnet-library-publish.yaml@cff152341e30d8a440fe87bd072c833c846b01e1 # v1.17.4
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
